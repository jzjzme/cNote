import { Dharma } from "../dharma";
export interface TokenData {
    symbol: string;
    balance: number;
    allowance: number;
    hasUnlimitedAllowance: boolean;
    name: string;
    numDecimals: number;
    address: string;
}
export declare class Tokens {
    private dharma;
    private readonly owner;
    constructor(dharma: Dharma, owner: string);
    get(): Promise<TokenData[]>;
    getTokenDataForSymbol(symbol: string): Promise<TokenData>;
    private getDataPromise;
}
